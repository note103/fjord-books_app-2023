<h1><%= t('helpers.titles.book.edit') %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('helpers.links.book.show'), @book %> |
  <%= link_to t('helpers.links.book.back'), books_path %>
</div>
