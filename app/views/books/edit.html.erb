<h1><%= t('helpers.titles.books.edit') %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t('helpers.links.books.show'), @book %> |
  <%= link_to t('helpers.links.books.back'), books_path %>
</div>
