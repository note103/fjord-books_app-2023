<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('helpers.links.edit', model: Book.model_name.human), edit_book_path(@book) %> |
  <%= link_to t('helpers.links.back', model: Book.model_name.human), books_path %>
  <%= button_to t('helpers.submit.destroy'), @book, method: :delete %>
</nav>
