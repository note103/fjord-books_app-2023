<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('helpers.links.books.edit'), edit_book_path(@book) %> |
  <%= link_to t('helpers.links.books.back'), books_path %>

  <%= button_to t('helpers.submit.destroy'), @book, method: :delete %>
</div>
